WildBeast fork. You probably want [upstream](https://github.com/TheSharks/WildBeast) instead.

Changes so far:
* Rule34 command stripped out
* e621 command query set to always use `-rating:e`, filtering out explicit results
